extends layout

block content
  h1= title

  ul
    each group in group_list
      li
        a(href=group.url) #{group.name}
        //-| (#{author.lifespan})
        

    else
      li There are no authors.